#like_form
  -if current_user.liking?(user)
    %button "見てね"
  -else
    =form_for(current_user.liking_likes.build(liking_id: user.id)) do |f|
      = f.hidden_field :liking_id
      = f.submit "いいね", class: "btn btn-large follow-btn"
